# cudson[rcusc2025]
# 📊 CUDSON [RCUSC2025] - Activity Score Processing

โปรเจกต์นี้คือสคริปต์ Python ที่พัฒนาขึ้นเพื่อจัดการ ทำความสะอาด และประมวลผล **"คะแนนกิจกรรมภายในมหาวิทยาลัยของนิสิตหอพัก"** โดยอัตโนมัติ สคริปต์จะทำการดึงข้อมูลการเข้าร่วมกิจกรรม (Check-in transactions) จากหลายแหล่ง นำมาประมวลผล จับคู่กับฐานข้อมูลรายชื่อนิสิต และสรุปผลออกมาเป็นไฟล์ Excel ที่พร้อมใช้งานทันที ช่วยลดระยะเวลาในการคำนวณคะแนนแบบ Manual

## ✨ ฟีเจอร์หลัก (Key Features)
* **Data Ingestion:** โหลดและอ่านข้อมูลจากไฟล์ Excel หลายไฟล์พร้อมกัน เช่น ข้อมูลประวัติการเช็คอินกิจกรรม (`check_in_transaction...xlsx`), ข้อมูลจาก Dashboard (`CUDSON_Dashboard_Data...xlsx`) และฐานข้อมูลรายชื่อเป้าหมาย (`รายชื่อส่งให้ กนห...xlsx`)
* **Data Transformation:** จัดการทำความสะอาดข้อมูล (Data Cleaning) และผสานข้อมูล (Merge/Join) จากตารางต่างๆ เข้าด้วยกันอย่างเป็นระบบ
* **Automated Scoring:** คำนวณคะแนนรวม (`final_score`) ของนิสิตแต่ละรายบุคคลได้อย่างแม่นยำ
* **Export to Excel:** ส่งออกผลลัพธ์เป็นไฟล์ Excel (`คะแนนกิจกรรมภายในมหาวิทยาลัยของนิสิตหอพักอ้างอิงรายชื่อนิสิต.xlsx`) โดยมีการจัดระเบียบ Sheet และ Header ให้พร้อมสำหรับนำไปส่งต่อให้หน่วยงานที่เกี่ยวข้อง

## 🛠️ เครื่องมือและไลบรารีที่ใช้ (Tech Stack)
* **Language:** Python 3
* **Data Science Libraries:** `pandas` (สำหรับการจัดการ Data Frame), `numpy` (สำหรับการประมวลผลเชิงตัวเลข)
* **Environment:** Google Colab / Jupyter Notebook

## 🚀 วิธีการรันโปรแกรม (How to Run)
เนื่องจากสคริปต์นี้ถูกออกแบบมาให้รันบน Google Colab สามารถทดสอบการทำงานได้ตามขั้นตอนดังนี้:
1. เปิดไฟล์ `cudson[rcusc2025].ipynb` บน Google Colab
2. อัปโหลดไฟล์ข้อมูลดิบ (Input Data) ที่จำเป็นลงใน Workspace ของ Colab ได้แก่:
   - ไฟล์ประวัติการเช็คอิน (Transaction)
   - ไฟล์ฐานข้อมูลรายชื่อนิสิต
3. เลือกเมนู `Runtime` > `Run all` เพื่อรันเซลล์ทั้งหมดตามลำดับ
4. เมื่อโปรแกรมทำงานเสร็จสิ้น จะปรากฏไฟล์ Output นามสกุล `.xlsx` ให้ดาวน์โหลดนำไปใช้งานต่อได้ทันที

## 👤 ติดต่อ / ผู้จัดทำ (Author)
* ตะวัน ขำสุข
* Email: khamsuk.taw@gmail.com
* LinkedIn : (https://www.linkedin.com/in/tawan-khamsuk-86890628b/)
